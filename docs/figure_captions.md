# Figure captions

## Figure 2. Coverage and AF/SR-classified performance
Coverage (proportions of AF/SR-classified, OA, UI, and Missing) and diagnostic performance (sensitivity, specificity, accuracy) of PCG and comparator devices against ECG12 reference labels on the matched cohort. Diagnostic metrics are computed only among AF/SR-classified outputs.

## Figure 3. PCG performance summary
Confusion matrix for the PCG RR-feature RandomForest classifier on the AF/SR-classified subset of the matched cohort. The ROC curve was generated from participant-level AF probabilities and is not included in the public repository. The included notebook regenerates the confusion matrix from aggregate counts when available.

## Figure 4. Representative RR-pattern examples
Excluded from public repository; included in submitted dissertation only because the figure is derived from protected participant-level signal data.

## Figure M1. Preprocessing example
Excluded from public repository; included in submitted dissertation only because the figure is derived from protected participant-level signal data.
## Figure M2. S1/S2 annotation and quality labeling
Excluded from public repository; included in submitted dissertation only because the figure is derived from protected participant-level signal data.
